import React from "react";
import t from "@/i18n/lang/zh/common";

const Loading: React.FC = () => (
   <div className="flex justify-center items-center h-[200px]">{t["加载中"]}...</div>
);
export default Loading;
